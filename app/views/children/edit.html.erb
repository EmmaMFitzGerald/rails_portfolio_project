Edit child
<% if @child.errors.any? %>
<h2>The following errors prevented the child from being changed</h2>
<ul>
<% @child.errors.full_messages.each do |msg| %>
<li><%= msg %></li>
<% end %>
</ul>
<% end %>

 <%= form_for(@child) do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :birthday %>
  <%= f.date_field :birthday %>

  <%= f.submit %>
<% end %>